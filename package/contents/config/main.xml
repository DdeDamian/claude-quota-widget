<?xml version="1.0" encoding="utf-8"?>
<kcfg xmlns="http://www.kde.org/standards/kcfg/1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.kde.org/standards/kcfg/1.0
      http://www.kde.org/standards/kcfg/1.0/kcfg.xsd">
  <kcfgfile name=""/>
  <group name="General">
    <!-- Data source: local (ccusage proxy) | online (claude.ai real %) | auto. -->
    <entry name="mode" type="String">
      <default>auto</default>
    </entry>
    <!-- Refresh cadence in seconds. -->
    <entry name="refreshSeconds" type="Int">
      <default>60</default>
    </entry>

    <!-- LOCAL: token caps used as % denominators. 0 = auto (p90 of history). -->
    <entry name="tokenLimit" type="Int">
      <default>0</default>
    </entry>
    <entry name="weeklyTokenLimit" type="Int">
      <default>0</default>
    </entry>

    <!-- ONLINE (preferred): OAuth Bearer token from `claude setup-token`. -->
    <entry name="token" type="String">
      <default></default>
    </entry>
    <!-- ONLINE (fallback): claude.ai cookie auth. -->
    <entry name="orgId" type="String">
      <default></default>
    </entry>
    <entry name="cookie" type="String">
      <default></default>
    </entry>
    <entry name="userAgent" type="String">
      <default></default>
    </entry>
  </group>
</kcfg>
